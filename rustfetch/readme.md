# finch's rustfetch config
My own customized config for rustfetch.

## About
I originally did use set the default look of rustfetch (which is why the `const` default values are what they are).
I thought it looked nice, but in order to differentiate which machine I ssh'd into; I did have to change the `config.toml`
to make distinguishing where I was a bit easier.
